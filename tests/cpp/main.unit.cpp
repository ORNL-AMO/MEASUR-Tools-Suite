#define CATCH_CONFIG_MAIN // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"

using namespace Catch;

/* Sample test case */
/*
TEST_CASE("Compare Equal Two Doubles Approximately", "[doubles=approx]") {
    CHECK(1.0000000001 == Approx(1.0));

    CHECK(1.0000000001 == Approx(1.0).epsilon(0.005));

    REQUIRE_THAT(1.0000000001, Matchers::WithinAbs(1.0, 0.005));
}
*/
